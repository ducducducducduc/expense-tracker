# expense-tracker
first attempt on expense tracking app


installed npm dependencies:
npm init -y => creates packages.json
npm instlal express cors => establish HTTP server, cors for frontend to call backend
npm install -D nodemon => allow auto-reload during dev 